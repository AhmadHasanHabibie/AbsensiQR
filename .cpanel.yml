---
deployment:
  tasks:
    - export DEPLOYPATH=/home/tokobiie/public_html/
    - /usr/bin/rsync -a --exclude='.git/' --exclude='.env' --exclude='vendor/' --exclude='storage/app/public/' ./ $DEPLOYPATH